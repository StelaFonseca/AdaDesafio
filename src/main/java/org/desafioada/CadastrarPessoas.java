package org.desafioada;
import java.util.Scanner;

/*public class CadastroDePessoa {
    //criar tela de cadastro .
    private Set<Pessoa> atores = new HashSet<>();
    private Set<Pessoa> diretores = new HashSet<>();

    public void addAtor() {
        Scanner scanner = new Scanner(System.in);
        System.out.println("NomeATOR: ");
        String NOME = scanner.nextLine();
        this.atores.add(new Ator(NOME, LocalDateTime.now()));
    }

    public Set<Pessoa> pesquisaPorNome(String nome) {
        return this.atores.stream()
                .filter(ator -> ator.getNome().equalsIgnoreCase(nome))
                .collect(Collectors.toSet());
    }


    public void imprimirAtores() {
        System.out.println(this.atores);
    }

    public void addDiretor(Pessoa pessoa) {
        this.diretores.add(pessoa);
    }
}*/

